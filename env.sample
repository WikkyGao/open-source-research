# open-source-research —— 平台凭证模板
#
# 用法：
#   cp env.sample .env     # 复制成 .env 后填写真实值
#   chmod 600 .env         # 收紧权限，避免同机其他用户读取
#
# .env 已被 .gitignore 忽略，不会被提交；env.sample 是模板，始终提交。
# 未配置的平台会走网页检索兜底，流程不会中断，只是结果质量下降。
#
# 脚本读取顺序（先命中者生效，已存在的环境变量优先级最高）：
#   1. --token-file 指定的文件
#   2. 技能目录下的 .env
#   3. ~/.workbuddy/oss-tokens.env
#   4. ~/.config/open-source-research/tokens.env
#
# 含占位符标记（{{ 、<你的 、<your 、xxxx 、TODO）的值会被自动忽略，
# 所以直接把本模板复制成 .env 也不会误发请求。

# GitHub：Settings → Developer settings → Personal access tokens
# 可选：gh CLI 已登录时不需要填
GITHUB_TOKEN={{你的 GitHub PAT}}

# CNB：https://cnb.cool → 头像 → 设置 → 访问令牌 → 添加访问令牌
# 权限必须勾选 repo-basic-info:r（公开仓库基础信息读取）
CNB_TOKEN={{你的 CNB 访问令牌}}

# Gitee：https://gitee.com → 设置 → 私人令牌 → 生成新令牌
# 权限勾选 projects（读取公开项目）
GITEE_TOKEN={{你的 Gitee 私人令牌}}
